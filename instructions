Step 1:  Install Homebrew
  ==> Open Mac Terminal and run the following Script
  ==> /usr/bin/ruby -e “$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/master/install)"
Step 2: Update SVN
  ==> Open Mac Terminal and run the following
  ==> brew install git svn
  To check the version of svn run the following
   ==> git --version or svn --version
